# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# ── 虚拟环境 ──
.venv/
venv/
env/

# ── 敏感配置（绝对不要提交！）──
config/.env
.env

# .env.example 是范例文件，可以提交
!.env.example
!config/.env.example

# ── PaddleOCR 模型缓存 ──
.paddleocr_cache/

# ── 运行时数据 ──
logs/
data/.kf_cursor.txt
data/.tunnel_url_kf.txt
data/failed/
data/inbox/
data/processed/

# ── 二进制文件 ──
cloudflared
gh

# ── GitHub CLI 临时数据 ──
.gh_state/

# ── Cloudflare Worker cache ──
.wrangler/

# ── 运行时数据库（不在 git 中管理）──
data/knowledge.db
data/chroma_db/

# ── macOS ──
.DS_Store
**/.DS_Store

# ── IDE / 工具配置 ──
.claude/
.vscode/
.idea/
*.swp
*.swo
data/boss_cookies.json
