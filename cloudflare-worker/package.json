{
  "name": "knowledge-agent-webhook",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute knowledge-agent-messages --file=./schema.sql",
    "db:query": "wrangler d1 execute knowledge-agent-messages --command"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.91.0",
    "typescript": "^5.6.0"
  }
}
